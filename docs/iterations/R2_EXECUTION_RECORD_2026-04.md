# R2 验收执行记录（桌面端手工）

> 对应 `iteration-02-canvas-and-nodes.md` §33–39  
> 执行环境：Tauri 桌面端（`npm run tauri dev`）  
> 每项通过后手动填写 ✓，失败 / 部分通过填 ✗ 并附注

---

## §33 手工步骤 1 — 五类节点创建与内容填写

| 步骤 | 节点 | 操作 | 预期结果 | 结果 |
|------|------|------|----------|------|
| 1a | 文本 | 从左侧「+」添加 TextNode | 节点出现，副标题显示「尚无正文；双击卡片或选中后编辑」 | [x] |
| 1b | 文本 | 双击 / 选中后编辑，输入一段文字 | 节点副标题变为文字首节（44字截断），正文保存成功 | [x] |
| 1c | 图片 | 添加 ImageAssetNode | 节点出现，副标题显示「未绑定素材；可上传或拖入工程」 | [x] |
| 1d | 视频 | 添加 VideoAssetNode | 同上，副标题一致 | [x] |
| 1e | 音频 | 添加 AudioAssetNode | 同上，副标题一致 | [x]（外观已按反馈优化） |
| 1f | 脚本 | 添加 ScriptNode | 节点出现，副标题显示「尚无脚本镜头；可写梗概或使用生成」 | [x] |

---

## §34 手工步骤 2 — 复制 / 粘贴 / 删除 / 连线

| 步骤 | 操作 | 预期结果 | 结果 |
|------|------|----------|------|
| 2a | 选中脚本节点，Ctrl+C | 状态栏提示「已复制 N 个」 | [x] |
| 2b | 在空白处 Ctrl+V | 新粘贴节点出现（位移 36px），脚本内 beatId 均为全新 UUID | [x] |
| 2c | 复制含 scriptBeatId 绑定的图片节点（及其上游脚本），粘贴 | 图片节点 `params.scriptBeatId` 同步指向副本内新 beatId | [x] |
| 2d | 选中任一节点，Delete | 节点与关联连线同步清除，状态栏提示「已删除 N 个」 | [x] |
| 2e | 建立文本→脚本连线（合法） | 连线成功落线，无类型错误提示 | [x] |
| 2f | 建立音频→脚本连线（非法） | 连线被拒绝，状态栏提示类型不匹配 | [x] |
| 2g | 多选（Shift+框选）后复制粘贴 | 多个节点均正确偏移，副标题/beatId 等状态正常 | [x] |

---

## §35 手工步骤 3 — 拖拽批量导入

| 步骤 | 操作 | 预期结果 | 结果 |
|------|------|----------|------|
| 3a | 从系统文件管理器拖入 3–5 张图片/视频/音频到画布 | 导入覆盖层出现，释放后节点错位排布（宫格，不堆叠） | [x] |
| 3b | 状态栏提示正确条数 | 显示「已导入 N 个」或「已导入 N 个，跳过 M 个不支持文件」 | [x] |
| 3c | 导入后节点被选中 | 成功选中本次导入的全部节点（可批量移动） | [x] |

---

## §36 手工步骤 4 — 混入不支持文件

| 步骤 | 操作 | 预期结果 | 结果 |
|------|------|----------|------|
| 4a | 拖入 2 个图片 + 1 个 `.txt` 或 `.exe` 文件 | 导入提示显示跳过数量；图片仍成功导入 | [x] |
| 4b | 状态栏提示可读 | 「跳过 N 个不支持文件」文案可理解 | [x] |

---

## §37 手工步骤 5 — 保存并重启恢复

| 步骤 | 操作 | 预期结果 | 结果 |
|------|------|----------|------|
| 5a | 新建工程，完整走一遍 1a–3b，保存 | `canvasflow.json` 存在且内容完整 | [x] |
| 5b | 关闭并重新打开工程目录 | 节点内容、素材路径、连线、视口全部恢复 | [x]（已补顶部关闭窗口按钮） |
| 5c | 检查脚本节点：beatId / scriptBeatSelection / storyboardShots | 勾选状态与保存前一致 | [x] |
| 5d | 检查图片节点：`params.scriptBeatId` 仍指向正确的 beatId | 绑定关系未断裂 | [x] |

---

## 自动化覆盖声明

以下已在 `npm run test` 中覆盖，无需手工重复：

| 测试文件 | 覆盖内容 |
|----------|----------|
| `r2ManualAcceptance.test.ts` | §30 节点初始 data；§31 连线合法性；粘贴 beatId 独立副本；disabled 边隔离 |
| `serialization.test.ts` | §34 等价：serialize/parse 回环后 scriptBeatId 不漂移 |
| `buildPasteNodesFromClipboard.test.ts` | 脚本→图片子图粘贴后 id 映射与连线重连正确性 |
| `nodeLayout.test.ts` | 批量导入宫格错位算法 |
| `nodeUiStrings.test.ts` | 各类节点空态副标题文案 |
| `pasteScriptBeatRemap.test.ts` | beatId remap、disabled 边 BFS 隔离 |

---

## 执行结论

- **总体结论**：`[x] 通过  [ ] 有条件通过  [ ] 阻断`
- P0 阻断项：无
- 高优先问题（可选记录）：无
- 备注：音频节点外观与关闭窗口按钮位置已根据人工反馈完成优化并通过回归。
